46: return 'Cripto'\n110: st.info("Ativos brasileiros (Ações/Fiis) recebem o sufixo .SA automaticamente. Para Stocks/Reits/Cripto, digite o ticker completo.")\n119: # Se não for Cripto conhecido sem hífen\n129: if tipo_inicial == 'Cripto' and '-' not in clean_name:\n136: if tipo_inicial in ['Cripto', 'Stocks', 'Reits']:\n316: compare_init = price_now_brl if current_type == 'Cripto' else price_now_native\n368: if asset_type == 'Cripto':\n399: # Para outros ativos (Ações, Fiis, Cripto), mantemos BRL\n428: asset_types = ["Ações", "Fiis", "Cripto", "Reits", "Stocks", "Renda Fixa"]\n436: if current_type == 'Cripto':\n784: edit_password = st.text_input("Nova Senha (deixe em branco para não alterar)", type="password", placeholder="*** (Criptografada)")\n1508: # Mapeamento de tickers para busca no Yahoo Finance (especialmente para Cripto)\n1513: if asset_r['asset_type'] == 'Cripto' and '-' not in t:\n1556: foreign_input_types = ['Stocks', 'Reits'] # Cripto é BRL por padrão no input (preço médio)\n1557: foreign_market_types = ['Cripto', 'Stocks', 'Reits'] # Cripto vem do YF em USD (BTC-USD)\n1626: # Calcular o total de ações/FIIs/Reits/Stocks (ignora Cripto e Renda Fixa)\n1674: # Quantidade formata 8 casas decimais para Cripto, 0 para o resto (como texto)\n1676: if row['Tipo'] == 'Cripto':\n1699: if row['asset_type'] == 'Cripto':\n1746: # Função de formatação de quantidade (8 casas para Cripto, 0 para outros)\n1748: if row['Tipo'] == 'Cripto':\n1761: if row['Tipo'] in ['Stocks', 'Reits', 'Cripto']: